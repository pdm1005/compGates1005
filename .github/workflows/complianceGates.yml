name: complianceGates

# Controls when the workflow will run
on:
  workflow_dispatch:
  push: 
    branches: ['main']
    
jobs:

  build:

    runs-on: self-hosted

    steps:
      -name: Checkout repository
       uses: actions/checkout@v2

      -name: Print hello world
       run: |
         echo "Hello world!"
